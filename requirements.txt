# Venu Cast — FY27 Budget Forecast
# Python 3.10+ (tested on 3.14)
# Install:   pip install -r requirements.txt

# Web framework
Flask>=3.0.0
flask-cors>=4.0.0
flask-login>=0.6.0
Werkzeug>=3.0.0

# Authentication
authlib>=1.3.0
python-dotenv>=1.0.0
PyJWT>=2.8.0

# Persistent storage (forecast version history) — we use Supabase's REST API
# directly via `requests` (declared below) to avoid the supabase-py dep tree.

# Data + math
numpy>=1.26.0
pandas>=2.1.0
openpyxl>=3.1.0          # Excel I/O (.xlsx)

# Forecasting models
prophet>=1.1.5           # Facebook Prophet (needs cmdstanpy)
cmdstanpy>=1.2.0
statsmodels>=0.14.0      # SARIMA fallback

# Australian public holidays
holidays>=0.40

# HTTP for weather (Open-Meteo) + test scripts
requests>=2.31.0
